# Implementation Plans

Generated by the improve skill on 2026-06-27 at commit `6bb6fa7`.

Execute in the order below unless dependencies say otherwise. Each executor should read the assigned plan fully before editing, run the drift check first, honor STOP conditions, and update this table when done.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| 001 | Reuse clean worktree-index stamps before CoW | P1 | M | - | DONE |
| 002 | Write clean workdir manifests from materialization stamps | P1 | M | - | DONE |
| 003 | Parallelize copy-on-write cloning | P1 | M | 002 | DONE |
| 004 | Stream root materialization in chunks | P2 | L | 001, 002 | DONE |

Status values: TODO, IN PROGRESS, DONE, BLOCKED with a one-line reason, or REJECTED with a one-line rationale.

## Dependency notes

- 001 and 002 are independent but should be benchmarked in this order to separate source-scan savings from destination-scan savings.
- 003 depends on 002 so parallel clone workers can return destination stamps through the same report path.
- 004 depends on 001 and 002 because streaming chunks need clean-index stamp reuse and final manifest assembly to avoid reintroducing full scans.

## Findings considered and deferred

- Sparse indexed CoW hydration: fold into 001 and 002 once selected-path stamp reuse and destination stamp reporting exist.
- Batch stale index pruning: useful for delete-heavy workloads, but lower impact for agent startup than source scans, destination scans, and serial cloning.
- Compression and path interning: reduce database size, but should follow materialization work so benchmark attribution stays clear.
- Raising the large materialization threshold: defer until 001 through 003 prove predictable startup on 100k and 1M file worktrees.

## Shared validation gate

Every plan has narrower checks. Before marking any plan DONE, also run:

```sh
make fmt-check
cargo check -p trail
cargo test -p trail
make bench-cli-scale-smoke
```
